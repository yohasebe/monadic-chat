include:
  - ruby/compose.yml
  - qdrant/compose.yml
  - embeddings/compose.yml
  - python/compose.yml
  - selenium/compose.yml
  - privacy/compose.yml

networks:
  monadic-chat-network:
    driver: bridge

volumes:
  data:
