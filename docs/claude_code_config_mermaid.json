{
  "mcpServers": {
    "monadic-chat-mermaid": {
      "command": "curl",
      "args": [
        "-X", "POST",
        "-H", "Content-Type: application/json",
        "-d", "@-",
        "-s",
        "-N",
        "http://localhost:3100/mcp"
      ],
      "env": {},
      "description": "Monadic Chat Mermaid Grapher MCP adapter for diagram validation and analysis"
    }
  }
}